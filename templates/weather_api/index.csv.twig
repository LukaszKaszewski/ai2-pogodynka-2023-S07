city,country,date,temperature,fahrenheit
{% for measurement in measurements %}
{{ city }},{{ country }},{{ measurement.date | date('Y-m-d') }},
{{ measurement.temperature }},{{ measurement.fahrenheit }}
{% endfor %}
